# Instala automaticamente as peer dependencies que faltarem (ex.: vite para o vitest).
auto-install-peers=true
# Recusa instalar em versao de Node fora do "engines" do package.json.
engine-strict=true
# Peers com versao levemente diferente viram aviso, nao erro.
strict-peer-dependencies=false
