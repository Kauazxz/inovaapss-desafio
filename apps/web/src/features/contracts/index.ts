/**
 * Feature de planos e contratos (Etapa 2).
 * - `PlansManager`: aba "Planos" em /settings.
 * - `ContractsPanel({ clientId })`: contratos de um cliente, para a tela do cliente (Etapa 9) embutir.
 */
export { ContractsPanel, type ContractsPanelProps } from './ContractsPanel';
export { PlansManager } from './PlansManager';
export * from './api';
